apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # This module provides Grafana dashboards and optional PodMonitor examples.
  # It does NOT install Prometheus or Grafana; those must be installed separately.
  - grafana-dashboard-basic.json
  - grafana-dashboard-advanced.json
